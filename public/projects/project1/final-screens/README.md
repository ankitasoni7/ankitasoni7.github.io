# project1 — final screens (three-phone showcase)

Drop three images here:

```
center.png   → tall, full-length screen (scrolls with the page in the large centre phone)
left.png     → tall screen (auto-scrolls; hover to pause & scroll it yourself)
right.png    → tall screen (auto-scrolls; hover to pause & scroll it yourself)
```

Referenced by the Final Screens section in `ProjectDetail.jsx`. Export each as one
**tall** PNG (~600px wide). Until the files exist, the phones show empty screens.
