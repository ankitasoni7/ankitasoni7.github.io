# High-fidelity screens — project1

Drop your **high-fidelity** phone screens here, named:

```
screen-01.png
screen-02.png
screen-03.png
```

They are referenced by `WireframeMorph.jsx` at:
`/projects/project1/high-fidelity/screen-0N.png`

Each high-fi screen should pair with the same-numbered mid-fi screen
(`screen-01` high reveals over `screen-01` mid, etc.). Portrait images
(~600×1300 / 9:19.5) look best in the iPhone frame.
