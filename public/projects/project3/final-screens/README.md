# project3 (Kaatkut) — final screens

Drop these images here:

```
desktop.png  → tall web screenshot for the MacBook mockup (auto-scrolls; hover to control)
center.png   → tall, full-length mobile screen (scrolls with the page in the large centre phone)
left.png     → tall mobile screen (auto-scrolls; hover to pause & scroll it yourself)
right.png    → tall mobile screen (auto-scrolls; hover to pause & scroll it yourself)
```

Referenced by the Final Screens section in `ProjectDetail3.jsx`.

- `desktop.png` should be a **tall** full-page website screenshot (e.g. ~1440px wide).
- The mobile screens should be tall PNGs (~600px wide).
- Until a file exists, that device shows an empty screen / placeholder.
