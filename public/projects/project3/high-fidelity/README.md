# Kaatkut — high-fidelity screens

Drop the final UI screens here, named:

```
screen-01.png
screen-02.png
screen-03.png
```

Used by `WireframeMorph project="project3"` — these are revealed (top → bottom clip)
as you scroll over each phone. Until a file exists, a placeholder is shown automatically.
