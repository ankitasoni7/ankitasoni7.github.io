# Kaatkut — mid-fidelity screens

Drop the wireframe screens here, named:

```
screen-01.png
screen-02.png
screen-03.png
```

Used by `WireframeMorph project="project3"` — each mid-fidelity screen morphs into
its matching `high-fidelity/screen-0N.png` as you scroll. Until a file exists, a
placeholder is shown automatically.
